apiVersion: v1
kind: Service
metadata:
  name: task-manager-api-service
spec:
  selector:
    app: task-manager-api
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080
